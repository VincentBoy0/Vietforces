---
phase: 02-auth-onboarding-sync-ux
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/example/vietforces/ui/screens/OnboardingScreen.kt
  - app/src/main/java/com/example/vietforces/navigation/Screen.kt
  - app/src/main/java/com/example/vietforces/data/storage/PreferencesManager.kt
  - app/src/main/java/com/example/vietforces/MainActivity.kt
  - app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-02

must_haves:
  truths:
    - "New users see 4-screen onboarding (Welcome → Choose Level → Choose Goal → Name+Avatar) on first launch — ONBOARD-01"
    - "Onboarding shows only once; onboarding_completed=true flag gates subsequent launches — ONBOARD-01"
    - "After onboarding, user enters guest mode and can access all game modes without creating an account — ONBOARD-02"
    - "Guest users see a Create Account banner card at top of ProfileScreen — ONBOARD-02"
    - "After completing at least 1 game session, guest sees a one-time dialog suggesting account creation — ONBOARD-02"
  artifacts:
    - path: "app/src/main/java/com/example/vietforces/ui/screens/OnboardingScreen.kt"
      provides: "4-step HorizontalPager with level/goal/name+avatar selection"
      exports: ["OnboardingScreen"]
    - path: "app/src/main/java/com/example/vietforces/data/storage/PreferencesManager.kt"
      provides: "onboarding_completed, is_guest, selected_level, daily_goal, guest_prompt_shown flags"
  key_links:
    - from: "MainActivity VietforcesApp startDestination"
      to: "Screen.Onboarding.route"
      via: "if (!PreferencesManager.getOnboardingCompleted()) use Onboarding as startDestination"
    - from: "OnboardingScreen finish callback"
      to: "PreferencesManager.setOnboardingCompleted(true) + setIsGuest(true)"
      via: "onFinish lambda executed on last step Confirm button click"
    - from: "ProfileScreen"
      to: "PreferencesManager.getIsGuest()"
      via: "var isGuest = remember { PreferencesManager.getIsGuest() }; if (isGuest) show GuestBannerCard"
---

<objective>
Deliver a 4-screen onboarding flow (per D decision: Welcome → Level → Goal → Name/Avatar) using
Compose HorizontalPager. On completion the user enters guest mode. PreferencesManager gains 5 new
flag keys. MainActivity start destination is determined by onboarding_completed. ProfileScreen shows
a persistent Create Account banner for guests, plus a one-time post-game dialog nudge.

Purpose: Users experience the app before creating an account, reducing registration friction.
Output: New OnboardingScreen.kt + additions to PreferencesManager.kt, Screen.kt, MainActivity.kt,
ProfileScreen.kt.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-onboarding-sync-ux/02-CONTEXT.md
@app/src/main/java/com/example/vietforces/navigation/Screen.kt
@app/src/main/java/com/example/vietforces/data/storage/PreferencesManager.kt
@app/src/main/java/com/example/vietforces/MainActivity.kt
@app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt
@app/src/main/java/com/example/vietforces/data/manager/UserProgressManager.kt
@app/src/main/java/com/example/vietforces/ui/theme/Color.kt
@.planning/phases/02-auth-onboarding-sync-ux/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PreferencesManager flags + OnboardingScreen (4-step HorizontalPager)</name>
  <files>
    app/src/main/java/com/example/vietforces/data/storage/PreferencesManager.kt,
    app/src/main/java/com/example/vietforces/ui/screens/OnboardingScreen.kt
  </files>
  <action>
    Step A — Update PreferencesManager.kt. Add 5 new constants and 10 new methods in the
    "// ==================== ONBOARDING / GUEST ====================" section (append after AI SETTINGS section):

    Constants:
      private const val KEY_ONBOARDING_COMPLETED = "onboarding_completed"
      private const val KEY_IS_GUEST = "is_guest"
      private const val KEY_SELECTED_LEVEL = "selected_level"
      private const val KEY_DAILY_GOAL = "daily_goal"
      private const val KEY_GUEST_PROMPT_SHOWN = "guest_game_prompt_shown"

    Methods:
      fun setOnboardingCompleted(completed: Boolean) { getPrefs().edit().putBoolean(KEY_ONBOARDING_COMPLETED, completed).apply() }
      fun getOnboardingCompleted(): Boolean = getPrefs().getBoolean(KEY_ONBOARDING_COMPLETED, false)
      fun setIsGuest(guest: Boolean) { getPrefs().edit().putBoolean(KEY_IS_GUEST, guest).apply() }
      fun getIsGuest(): Boolean = getPrefs().getBoolean(KEY_IS_GUEST, false)
      fun setSelectedLevel(level: String) { getPrefs().edit().putString(KEY_SELECTED_LEVEL, level).apply() }
      fun getSelectedLevel(): String = getPrefs().getString(KEY_SELECTED_LEVEL, "beginner") ?: "beginner"
      fun setDailyGoal(goal: Int) { getPrefs().edit().putInt(KEY_DAILY_GOAL, goal).apply() }
      fun getDailyGoal(): Int = getPrefs().getInt(KEY_DAILY_GOAL, 10)
      fun setGuestPromptShown(shown: Boolean) { getPrefs().edit().putBoolean(KEY_GUEST_PROMPT_SHOWN, shown).apply() }
      fun getGuestPromptShown(): Boolean = getPrefs().getBoolean(KEY_GUEST_PROMPT_SHOWN, false)

    Step B — Create OnboardingScreen.kt at package com.example.vietforces.ui.screens:
    - Imports: androidx.compose.foundation.pager.HorizontalPager, rememberPagerState,
      androidx.compose.animation.core.*, androidx.compose.foundation.layout.*, etc.
    - Signature: @Composable fun OnboardingScreen(onFinish: () -> Unit)
    - Local state:
        val pagerState = rememberPagerState(pageCount = { 4 })
        var selectedLevel by remember { mutableStateOf("beginner") }
        var selectedGoal by remember { mutableIntStateOf(10) }
        var userName by remember { mutableStateOf("") }
        var selectedAvatar by remember { mutableStateOf("🦁") }
        val coroutineScope = rememberCoroutineScope()
        val context = LocalContext.current
    - Root layout: Column(Modifier.fillMaxSize().background(MaterialTheme.colorScheme.background)):
        - HorizontalPager(state=pagerState, modifier=Modifier.weight(1f), userScrollEnabled=false):
            Page 0 — WelcomePage (composable defined in same file)
            Page 1 — ChooseLevelPage(selectedLevel) { selectedLevel = it }
            Page 2 — ChooseGoalPage(selectedGoal) { selectedGoal = it }
            Page 3 — NameAvatarPage(userName, selectedAvatar, onNameChange={userName=it}, onAvatarChange={selectedAvatar=it})
        - PagerDotIndicator(pagerState) — Row of 4 dots, filled dot for current page
        - Bottom action row with padding(16.dp):
            if (pagerState.currentPage < 3):
                Button(fillMaxWidth, onClick={ coroutineScope.launch { pagerState.animateScrollToPage(pagerState.currentPage + 1) } }) { Text("Tiếp theo →") }
            else:
                Button(fillMaxWidth, onClick={
                    PreferencesManager.setOnboardingCompleted(true)
                    PreferencesManager.setIsGuest(true)
                    PreferencesManager.setSelectedLevel(selectedLevel)
                    PreferencesManager.setDailyGoal(selectedGoal)
                    ProfileManager.updateName(userName.ifBlank { "Chiến binh" })
                    onFinish()
                }, enabled=userName.isNotBlank()) { Text("Bắt đầu học!") }

    Private page composables (in same file):

    @Composable private fun WelcomePage():
    - Column(fillMaxSize, horizontalAlignment=CenterHorizontally, verticalArrangement=Center, padding(32.dp)):
      - Text("🐓", fontSize=80.sp)
      - Spacer(16.dp)
      - Text("VietForces", fontSize=36.sp, fontWeight=Bold, color=VietRed)
      - Spacer(8.dp)
      - Text("Học từ vựng tiếng Việt\nqua trò chơi thú vị", fontSize=18.sp, color=TextSecondary, textAlign=Center)
      - Spacer(24.dp)
      - Text("🎮 7 chế độ chơi đặc sắc", fontSize=15.sp, color=TextPrimary)
      - Text("🏆 Bảng xếp hạng ELO toàn cầu", fontSize=15.sp, color=TextPrimary)
      - Text("🔥 Chuỗi ngày học liên tiếp", fontSize=15.sp, color=TextPrimary)

    @Composable private fun ChooseLevelPage(selectedLevel: String, onLevelSelect: (String) -> Unit):
    - Column(fillMaxSize, padding(24.dp)):
      - Text("Trình độ của bạn?", fontSize=24.sp, fontWeight=Bold)
      - Spacer(8.dp)
      - Text("Chúng tôi sẽ điều chỉnh độ khó phù hợp", fontSize=14.sp, color=TextSecondary)
      - Spacer(24.dp)
      - For each level in listOf("beginner" to "🌱 Người mới bắt đầu" + "Chưa biết gì về tiếng Việt", "intermediate" to "🌿 Trung cấp" + "Biết một số từ cơ bản", "advanced" to "🌳 Nâng cao" + "Đã học tiếng Việt một thời gian"):
          LevelCard(id, label, subtitle, isSelected=selectedLevel==id, onClick={onLevelSelect(id)})
    - LevelCard: Card with border color VietRed if selected else transparent, padding(16.dp), Row with emoji + Column(name + subtitle)

    @Composable private fun ChooseGoalPage(selectedGoal: Int, onGoalSelect: (Int) -> Unit):
    - Column(fillMaxSize, padding(24.dp)):
      - Text("Mục tiêu mỗi ngày?", fontSize=24.sp, fontWeight=Bold)
      - Spacer(8.dp); Text("Bạn muốn học bao nhiêu từ mỗi ngày?", fontSize=14.sp, color=TextSecondary); Spacer(24.dp)
      - For each goal in listOf(5 to "🐢 5 từ/ngày" + "Nhẹ nhàng", 10 to "🚶 10 từ/ngày" + "Cân bằng (khuyến nghị)", 15 to "🏃 15 từ/ngày" + "Thử thách"):
          GoalCard(goal, label, subtitle, isSelected=selectedGoal==goal, onClick={onGoalSelect(goal)})

    @Composable private fun NameAvatarPage(name, avatar, onNameChange, onAvatarChange):
    - Column(fillMaxSize, padding(24.dp)):
      - Text("Bạn là ai?", fontSize=24.sp, fontWeight=Bold)
      - Spacer(8.dp); Text("Đặt tên và chọn avatar chiến binh của bạn", fontSize=14.sp, color=TextSecondary); Spacer(24.dp)
      - Text(avatar, fontSize=64.sp, modifier=Modifier.align(CenterHorizontally))
      - Spacer(16.dp)
      - LazyRow (or Row with horizontalScroll) of avatar emoji chips:
          val avatars = listOf("🦁","🐯","🦊","🐺","🦅","🐉","⚔️","🛡️")
          For each a: FilterChip(selected=avatar==a, onClick={onAvatarChange(a)}, label={Text(a, fontSize=24.sp)})
      - Spacer(24.dp)
      - OutlinedTextField(value=name, onValueChange=onNameChange, label={Text("Tên của bạn")}, modifier=fillMaxWidth, singleLine=true, placeholder={Text("Chiến binh Việt")})

    @Composable private fun PagerDotIndicator(pagerState: PagerState):
    - Row(horizontalArrangement=spacedBy(8.dp), modifier=Modifier.fillMaxWidth().padding(vertical=12.dp), horizontalAlignment=CenterHorizontally):
      - Repeat(4) { index -> Box(Modifier.size(if(pagerState.currentPage==index) 10.dp else 8.dp).clip(CircleShape).background(if(pagerState.currentPage==index) VietRed else Color.LightGray)) }
  </action>
  <verify>
    <automated>cd /Users/longtdang/Long/HCMUS/vietforces && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>PreferencesManager has 5 new keys and 10 new methods. OnboardingScreen compiles with HorizontalPager, 4 page composables, dot indicator, and finish callback that writes all prefs flags and calls onFinish().</done>
</task>

<task type="auto">
  <name>Task 2: Wire Onboarding Start Destination + Guest Mode in ProfileScreen</name>
  <files>
    app/src/main/java/com/example/vietforces/navigation/Screen.kt,
    app/src/main/java/com/example/vietforces/MainActivity.kt,
    app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt
  </files>
  <action>
    Update Screen.kt — add after object Register (added in Plan 01):
      object Onboarding : Screen("onboarding")

    Update MainActivity.kt — three changes:
    1. Determine dynamic startDestination at the top of VietforcesApp() composable:
         val onboardingCompleted = remember { PreferencesManager.getOnboardingCompleted() }
         val startDestination = if (!onboardingCompleted) Screen.Onboarding.route else Screen.Main.route
       Import com.example.vietforces.data.storage.PreferencesManager.
    2. Change NavHost startDestination from the hardcoded Screen.Main.route to the dynamic startDestination variable.
    3. Add composable route for Onboarding inside NavHost (place first, before composable(Screen.Main.route)):
         composable(Screen.Onboarding.route) {
             OnboardingScreen(
                 onFinish = {
                     navController.navigate(Screen.Main.route) {
                         popUpTo(Screen.Onboarding.route) { inclusive = true }
                     }
                 }
             )
         }
       Add import com.example.vietforces.ui.screens.OnboardingScreen.
    4. Add Screen.Onboarding.route to the NOT-in-bottom-nav list: the current showBottomNav check already excludes any route not in the allowlist. Confirm Screen.Onboarding.route is not in the listOf(Screen.Main.route, Screen.Profile.route, Screen.Settings.route, Screen.Performance.route) — no change needed.

    Update ProfileScreen.kt — three changes:
    1. At the top of the ProfileScreen composable body, read guest flags:
         val isGuest = remember { PreferencesManager.getIsGuest() }
         val promptAlreadyShown = remember { PreferencesManager.getGuestPromptShown() }
         val totalGamesPlayed = remember { UserProgressManager.getAllGameModeStats().values.sumOf { it.gamesPlayed } }
         var showGuestDialog by remember { mutableStateOf(false) }
       Import com.example.vietforces.data.storage.PreferencesManager.
    2. Show one-time post-game dialog: after the remember blocks add:
         LaunchedEffect(isGuest, totalGamesPlayed, promptAlreadyShown) {
             if (isGuest && totalGamesPlayed >= 1 && !promptAlreadyShown) {
                 showGuestDialog = true
             }
         }
         if (showGuestDialog) {
             AlertDialog(
                 onDismissRequest = { showGuestDialog = false; PreferencesManager.setGuestPromptShown(true) },
                 title = { Text("🎉 Bạn đang tiến bộ!") },
                 text = { Text("Tạo tài khoản miễn phí để lưu tiến độ lên cloud và không bao giờ mất dữ liệu.") },
                 confirmButton = { TextButton(onClick = { /* navigation handled externally — set flag and close */ PreferencesManager.setGuestPromptShown(true); showGuestDialog = false }) { Text("Để sau", color=TextSecondary) } },
                 dismissButton = { Button(onClick = { PreferencesManager.setGuestPromptShown(true); showGuestDialog = false }) { Text("Đăng ký ngay") } }
             )
         }
       Note: "Đăng ký ngay" navigates to Register — ProfileScreen needs an onNavigateToRegister: () -> Unit = {} parameter for this. Add it to the function signature and wire in MainActivity:
         composable(Screen.Profile.route) { ProfileScreen(onNavigateToRegister = { navController.navigate(Screen.Register.route) }) }
    3. In the Column body of ProfileScreen, before ProfileHeaderCard, add the guest banner card (only when isGuest=true):
         if (isGuest) {
             Card(modifier=Modifier.fillMaxWidth(), shape=RoundedCornerShape(16.dp),
                  colors=CardDefaults.cardColors(containerColor=VietRed.copy(alpha=0.08f)),
                  border=BorderStroke(1.dp, VietRed.copy(alpha=0.3f))) {
                 Row(modifier=Modifier.fillMaxWidth().padding(16.dp), verticalAlignment=CenterVertically) {
                     Column(modifier=Modifier.weight(1f)) {
                         Text("💾 Lưu tiến độ lên cloud", fontSize=14.sp, fontWeight=FontWeight.SemiBold, color=VietRed)
                         Text("Tạo tài khoản miễn phí để không mất dữ liệu", fontSize=12.sp, color=TextSecondary)
                     }
                     Spacer(8.dp)
                     Button(onClick={ PreferencesManager.setGuestPromptShown(true); onNavigateToRegister() },
                            colors=ButtonDefaults.buttonColors(containerColor=VietRed), contentPadding=PaddingValues(horizontal=12.dp, vertical=6.dp)) {
                         Text("Đăng ký", fontSize=13.sp)
                     }
                 }
             }
         }
       Add imports: androidx.compose.foundation.BorderStroke, com.example.vietforces.data.storage.PreferencesManager, com.example.vietforces.data.manager.UserProgressManager.
  </action>
  <verify>
    <automated>cd /Users/longtdang/Long/HCMUS/vietforces && ./gradlew :app:assembleDebug 2>&1 | tail -30</automated>
  </verify>
  <done>assembleDebug succeeds. Screen.kt has Onboarding route. NavHost uses dynamic startDestination based on onboarding_completed flag. OnboardingScreen is the first screen new users see. ProfileScreen shows a VietRed banner for guest users and a one-time AlertDialog after first game completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OnboardingScreen userName input → ProfileManager | User-supplied display name; stored locally |
| PreferencesManager flags → Navigation logic | Flags control which screen users see first; must not be bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | OnboardingScreen userName field | accept | Display name is cosmetic only; stored in ProfileManager locally; no auth impact |
| T-02-07 | Tampering | onboarding_completed SharedPreferences flag | accept | Flag controls first-launch UX only, not security gates; a cleared flag just re-shows onboarding |
| T-02-08 | Elevation of Privilege | Guest mode bypass | accept | Guest mode grants access to the same app content as authenticated users; there is no privileged content behind auth that guests could bypass |
| T-02-SC | Tampering | No new package installs | accept | HorizontalPager is from the existing Compose BOM 2024.09.00; no new install required |
</threat_model>

<verification>
1. ./gradlew :app:assembleDebug — BUILD SUCCESSFUL
2. grep "onboarding" app/src/main/java/com/example/vietforces/data/storage/PreferencesManager.kt — shows KEY_ONBOARDING_COMPLETED, setOnboardingCompleted, getOnboardingCompleted
3. grep "Screen.Onboarding" app/src/main/java/com/example/vietforces/MainActivity.kt — shows startDestination logic
4. grep "isGuest" app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt — shows banner card condition
5. grep "HorizontalPager" app/src/main/java/com/example/vietforces/ui/screens/OnboardingScreen.kt — present
</verification>

<success_criteria>
- PreferencesManager has getOnboardingCompleted/setOnboardingCompleted, getIsGuest/setIsGuest, getSelectedLevel/setSelectedLevel, getDailyGoal/setDailyGoal, getGuestPromptShown/setGuestPromptShown
- OnboardingScreen uses HorizontalPager with 4 pages; userScrollEnabled=false (navigation via button only)
- Page 1 shows 3 level cards (beginner/intermediate/advanced) with selection state
- Page 2 shows 3 goal cards (5/10/15 words/day) with selection state
- Page 3 has userName OutlinedTextField + emoji avatar chips
- Finish writes all prefs flags and calls onFinish(); "Bắt đầu học!" button disabled when userName is blank
- MainActivity startDestination is Screen.Onboarding.route when onboarding_completed=false
- ProfileScreen shows VietRed Create Account banner when isGuest=true
- One-time AlertDialog appears in ProfileScreen when isGuest=true && totalGamesPlayed >= 1 && !guestPromptShown
</success_criteria>

<output>
Create .planning/phases/02-auth-onboarding-sync-ux/02-02-SUMMARY.md when done
</output>
