---
phase: 03-streak-leaderboard
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/com/example/vietforces/ui/components/StreakHeatmapComposable.kt
  - app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt
  - app/src/main/java/com/example/vietforces/data/worker/StreakDangerWorker.kt
  - app/src/main/java/com/example/vietforces/VietForcesApplication.kt
autonomous: true
requirements:
  - STREAK-02
  - STREAK-04

must_haves:
  truths:
    - "StreakHeatmapComposable renders a 4×7 grid of the last 28 days; practiced days colored VietRed, absent days light gray"
    - "Heatmap is embedded in ProfileScreen below ProfileStatsCard"
    - "StreakDangerWorker fires after 22:00 local time when last_practice_date != today"
    - "WorkManager periodic task is enqueued once (KEEP policy) from VietForcesApplication.onCreate()"
    - "StreakDangerWorker posts a system notification via NotificationCompat using channel ID streak_danger"
  artifacts:
    - path: "app/src/main/java/com/example/vietforces/ui/components/StreakHeatmapComposable.kt"
      provides: "28-day streak heatmap composable"
      exports: ["StreakHeatmapComposable"]
    - path: "app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt"
      provides: "Heatmap embedded below ProfileStatsCard"
      contains: "StreakHeatmapComposable"
    - path: "app/src/main/java/com/example/vietforces/data/worker/StreakDangerWorker.kt"
      provides: "Hourly WorkManager task for streak danger alert"
      exports: ["StreakDangerWorker"]
    - path: "app/src/main/java/com/example/vietforces/VietForcesApplication.kt"
      provides: "WorkManager.enqueueUniquePeriodicWork registered on app start"
      contains: "enqueueUniquePeriodicWork"
  key_links:
    - from: "ProfileScreen"
      to: "StreakHeatmapComposable"
      via: "produceState { StreakRepository.instance?.getStreakHistory(28) } then pass to composable"
      pattern: "StreakHeatmapComposable"
    - from: "StreakDangerWorker.doWork()"
      to: "NotificationCompat.Builder"
      via: "system notification posted when 22:00 check passes"
      pattern: "NotificationCompat\\.Builder"
    - from: "VietForcesApplication.onCreate()"
      to: "WorkManager.enqueueUniquePeriodicWork"
      via: "PeriodicWorkRequestBuilder<StreakDangerWorker>(1, HOURS)"
      pattern: "enqueueUniquePeriodicWork"
---

<objective>
Deliver the streak heatmap UI (STREAK-04) in ProfileScreen and the streak-danger background worker (STREAK-02). Depends on 03-02 (StreakRepository.instance must be set). Requires adding WorkManager to the Gradle dependency catalog.

Purpose: These are the two highest-retention touchpoints visible to the user daily: the 28-day heatmap makes progress tangible, and the 22:00 alert prevents streak breaks through timely nudging.

Output: Two new Kotlin files (StreakHeatmapComposable.kt, StreakDangerWorker.kt), one modified screen (ProfileScreen.kt), one modified Application class (VietForcesApplication.kt), and WorkManager dependency added to Gradle.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-streak-leaderboard/03-CONTEXT.md
@app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt
@app/src/main/java/com/example/vietforces/VietForcesApplication.kt
@app/src/main/java/com/example/vietforces/data/manager/NotificationManager.kt
@app/src/main/java/com/example/vietforces/data/repository/StreakRepository.kt
@gradle/libs.versions.toml
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkManager deps + StreakDangerWorker + VietForcesApplication registration</name>
  <files>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    app/src/main/java/com/example/vietforces/data/worker/StreakDangerWorker.kt,
    app/src/main/java/com/example/vietforces/VietForcesApplication.kt
  </files>
  <action>
    Step 1 — Add WorkManager to `gradle/libs.versions.toml`:
    In the `[versions]` section add: `work = "2.9.1"`.
    In the `[libraries]` section add: `work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "work" }`.

    Step 2 — Add WorkManager to `app/build.gradle.kts`:
    In the `dependencies {}` block, add `implementation(libs.work.runtime.ktx)` immediately after the `implementation(libs.lifecycle.runtime.compose)` line.

    Step 3 — Create `StreakDangerWorker.kt` in `com.example.vietforces.data.worker` (create the `worker` package directory):

    `class StreakDangerWorker(appContext: Context, params: WorkerParameters) : CoroutineWorker(appContext, params)`.

    Define companion constants: `const val CHANNEL_ID = "streak_danger"`, `const val NOTIFICATION_ID = 1001`, `const val WORK_NAME = "streak_danger_check"`.

    Override `suspend fun doWork(): Result`:

    1. Check the current local hour: `val hour = Calendar.getInstance().get(Calendar.HOUR_OF_DAY)`. If `hour < 22` return `Result.success()` (too early, nothing to do).

    2. Get last practiced date: `val lastPracticed = PreferencesManager.getLastPracticeDate()` — read from SharedPreferences. The key used in `PreferencesManager` for this date value is whatever field stores `UserProgressManager.getUserSession().lastPracticeDate`. If `PreferencesManager` does not have a direct `getLastPracticeDate()` method, call `UserProgressManager.getUserSession().lastPracticeDate` directly (it is an in-memory value loaded at app start from SharedPreferences).

    3. Get today's date string in UTC using `SimpleDateFormat("yyyy-MM-dd", Locale.ROOT)` with `TimeZone.getTimeZone("UTC")` applied (per PRE-01).

    4. If `lastPracticed == todayUtc` return `Result.success()` (already practiced today).

    5. Create notification channel (required for Android 8+): obtain `NotificationManagerCompat.from(applicationContext)`; create `NotificationChannel(CHANNEL_ID, "Streak Alert", NotificationManager.IMPORTANCE_HIGH)` and call `notificationManager.createNotificationChannel(channel)` inside `if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O)` block.

    6. Build notification: `NotificationCompat.Builder(applicationContext, CHANNEL_ID)` with `.setSmallIcon(R.drawable.ic_launcher_foreground)`, `.setContentTitle("⚠️ Streak sắp bị mất!")`, `.setContentText("Bạn chưa học hôm nay. Hãy chơi 1 game để giữ chuỗi ngày!")`, `.setPriority(NotificationCompat.PRIORITY_HIGH)`, `.setAutoCancel(true)`.

    7. Post notification: `NotificationManagerCompat.from(applicationContext).notify(NOTIFICATION_ID, notification.build())` — wrap in try/catch for `SecurityException` (Android 13+ permission not granted: return `Result.success()` silently rather than `Result.failure()`).

    8. Return `Result.success()`.

    Imports: `android.app.NotificationChannel`, `android.app.NotificationManager`, `android.content.Context`, `android.os.Build`, `androidx.core.app.NotificationCompat`, `androidx.core.app.NotificationManagerCompat`, `androidx.work.CoroutineWorker`, `androidx.work.WorkerParameters`, `com.example.vietforces.data.manager.UserProgressManager`, `com.example.vietforces.data.storage.PreferencesManager`, `java.text.SimpleDateFormat`, `java.util.*`.

    Step 4 — Modify `VietForcesApplication.kt`:
    In `onCreate()`, after all existing manager initializations, add the WorkManager registration block:
    ```
    val streakWorkRequest = PeriodicWorkRequestBuilder<StreakDangerWorker>(1, TimeUnit.HOURS)
        .build()
    WorkManager.getInstance(this).enqueueUniquePeriodicWork(
        StreakDangerWorker.WORK_NAME,
        ExistingPeriodicWorkPolicy.KEEP,
        streakWorkRequest
    )
    ```
    Add imports: `androidx.work.ExistingPeriodicWorkPolicy`, `androidx.work.PeriodicWorkRequestBuilder`, `androidx.work.WorkManager`, `com.example.vietforces.data.worker.StreakDangerWorker`, `java.util.concurrent.TimeUnit`.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>WorkManager dependency resolves. StreakDangerWorker compiles as CoroutineWorker. VietForcesApplication.onCreate() contains enqueueUniquePeriodicWork with KEEP policy. Notification is posted only when hour >= 22 AND last_practiced != today UTC.</done>
</task>

<task type="auto">
  <name>Task 2: StreakHeatmapComposable + embed in ProfileScreen</name>
  <files>
    app/src/main/java/com/example/vietforces/ui/components/StreakHeatmapComposable.kt,
    app/src/main/java/com/example/vietforces/ui/screens/ProfileScreen.kt
  </files>
  <action>
    Create `StreakHeatmapComposable.kt` in `com.example.vietforces.ui.components`:

    `@Composable fun StreakHeatmapComposable(practicedDates: Set<String>, modifier: Modifier = Modifier)`.

    Where `practicedDates` is a Set of "YYYY-MM-DD" strings representing days the user practiced.

    Layout:
    - Outer: `Card(modifier = modifier.fillMaxWidth(), shape = RoundedCornerShape(16.dp), elevation = CardDefaults.cardElevation(defaultElevation = 2.dp))`.
    - Inside card: `Column(modifier = Modifier.fillMaxWidth().padding(16.dp), verticalArrangement = Arrangement.spacedBy(8.dp))`.
    - Header row: `Text("Lịch học", fontWeight = FontWeight.SemiBold, fontSize = 16.sp, color = TextPrimary)`.
    - Day-of-week header: `Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceEvenly)` with 7 `Text` items: "T2", "T3", "T4", "T5", "T6", "T7", "CN" (Mon–Sun). Each text: `fontSize = 10.sp, color = TextSecondary, textAlign = TextAlign.Center`.
    - Grid: Build a list of 28 date strings covering the last 28 days. Compute `val calendar = Calendar.getInstance().apply { timeZone = TimeZone.getTimeZone("UTC") }` and `val sdf = SimpleDateFormat("yyyy-MM-dd", Locale.ROOT).apply { timeZone = TimeZone.getTimeZone("UTC") }`. Build the 28-day list by iterating `i in 27 downTo 0`: clone calendar, subtract i days, format as string. The result is ordered oldest-first.
    - Pad the list to align to Monday: compute `dayOfWeekOffset` = `dayOfWeek - Calendar.MONDAY` (mod 7) for the first date in the list. Prepend `dayOfWeekOffset` null entries to the list so day 0 falls on the correct column. Then chunk the padded list into rows of 7 and render each row as a `Row(horizontalArrangement = Arrangement.SpaceEvenly)`.
    - Each cell: `Box(modifier = Modifier.size(32.dp).clip(RoundedCornerShape(6.dp)).background(color = if date != null && date in practicedDates then VietRed else if date != null then Color(0xFFE0E0E0) else Color.Transparent))`. Inside the box (for practiced days): optionally show a small "🔥" emoji `Text` at `fontSize = 10.sp` centered.
    - Below grid: a legend row with two `Box` samples: practiced (VietRed) labeled "Đã học" and absent (Color(0xFFE0E0E0)) labeled "Chưa học", both `fontSize = 10.sp, color = TextSecondary`.

    Imports: `androidx.compose.foundation.background`, `androidx.compose.foundation.layout.*`, `androidx.compose.foundation.shape.RoundedCornerShape`, `androidx.compose.material3.*`, `androidx.compose.runtime.*`, `androidx.compose.ui.Alignment`, `androidx.compose.ui.Modifier`, `androidx.compose.ui.draw.clip`, `androidx.compose.ui.graphics.Color`, `androidx.compose.ui.text.font.FontWeight`, `androidx.compose.ui.text.style.TextAlign`, `androidx.compose.ui.unit.dp`, `androidx.compose.ui.unit.sp`, `com.example.vietforces.ui.theme.*`, `java.text.SimpleDateFormat`, `java.util.*`.

    ---

    Modify `ProfileScreen.kt`:

    Add imports at the top: `androidx.compose.runtime.produceState`, `com.example.vietforces.data.repository.StreakRepository`, `com.example.vietforces.ui.components.StreakHeatmapComposable`.

    Inside the `ProfileScreen` composable, after the existing variable declarations but before the outer `Column`, add:

    ```
    val practicedDates by produceState<Set<String>>(initialValue = emptySet()) {
        value = StreakRepository.instance
            ?.getStreakHistory(28)
            ?.getOrNull()
            ?.map { it.practicedDate }
            ?.toSet()
            ?: emptySet()
    }
    ```

    In the inner `Column` (the scrollable one with `verticalArrangement = Arrangement.spacedBy(16.dp)`), add `StreakHeatmapComposable(practicedDates = practicedDates)` immediately after the `ProfileStatsCard(userSession = userSession)` call and before the `RankProgressCard` call.

    The `ProfileStatsCard` call is on the line `ProfileStatsCard(userSession = userSession)`. Insert the heatmap composable immediately after it. The `verticalArrangement = Arrangement.spacedBy(16.dp)` already handles spacing.

    Do NOT restructure the existing ProfileScreen layout — only add the heatmap and the produceState block.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>StreakHeatmapComposable compiles. ProfileScreen contains StreakHeatmapComposable call after ProfileStatsCard. produceState loads StreakRepository.instance data. STREAK-04: 28-day heatmap rendered in ProfileScreen. STREAK-02: StreakDangerWorker fires after 22:00 when not practiced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkManager → local SharedPreferences | StreakDangerWorker reads last_practice_date from local SharedPreferences — no network call |
| Notification POST_NOTIFICATIONS | Android 13+ requires runtime permission; worker handles SecurityException silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | Streak heatmap data | accept | streak_history RLS allows only own-user SELECT; no other user's data can be loaded |
| T-03-12 | Denial of Service | WorkManager hourly task | mitigate | ExistingPeriodicWorkPolicy.KEEP prevents duplicate enqueues across app restarts; notification deduplication via fixed NOTIFICATION_ID = 1001 |
| T-03-13 | Tampering | Worker local time check | accept | Worker checks LOCAL Calendar.HOUR_OF_DAY for 22:00 alert — intentionally local time per STREAK-02 spec; UTC used only for date string comparison |
| T-03-SC | Tampering | WorkManager dependency install | mitigate | work-runtime-ktx 2.9.1 is a well-known AndroidX library; verify artifact group `androidx.work` on Maven Central before applying |
</threat_model>

<verification>
1. `./gradlew :app:compileDebugKotlin` — zero errors.
2. Inspect `StreakDangerWorker.kt`: confirm `hour < 22` early-return guard; `Locale.ROOT` in SimpleDateFormat; `SecurityException` caught on notify().
3. Inspect `VietForcesApplication.kt`: confirm `enqueueUniquePeriodicWork(WORK_NAME, KEEP, ...)` present in `onCreate()`.
4. Inspect `StreakHeatmapComposable.kt`: confirm `SimpleDateFormat("yyyy-MM-dd", Locale.ROOT)` with UTC timezone; 28-day list generation; VietRed for practiced days.
5. Inspect `ProfileScreen.kt`: confirm `produceState` block loading `StreakRepository.instance?.getStreakHistory(28)`; `StreakHeatmapComposable(practicedDates = practicedDates)` placed after ProfileStatsCard.
6. Run on device/emulator: advance clock to 22:05, ensure `last_practice_date` is yesterday → notification appears.
</verification>

<success_criteria>
- STREAK-02: Worker fires hourly; posts notification "⚠️ Streak sắp bị mất!" after 22:00 when user has not practiced today ✓
- STREAK-04: 28-day heatmap visible in ProfileScreen below stats card; practiced dates highlighted in VietRed ✓
- WorkManager enqueued with KEEP policy — no duplicate tasks across restarts ✓
- StreakDangerWorker handles Android 13+ notification permission gracefully (no crash on SecurityException) ✓
- `./gradlew :app:compileDebugKotlin` passes with zero errors ✓
</success_criteria>

<output>
Create `.planning/phases/03-streak-leaderboard/03-04-SUMMARY.md` when done
</output>
