---
phase: 03-streak-leaderboard
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/src/main/java/com/example/vietforces/data/repository/EloRepository.kt
  - app/src/main/java/com/example/vietforces/data/repository/StreakRepository.kt
  - app/src/main/java/com/example/vietforces/di/GameModule.kt
  - app/src/main/java/com/example/vietforces/data/repository/ProgressRepository.kt
autonomous: true
requirements:
  - ELO-01
  - ELO-02
  - STREAK-01
  - STREAK-02
  - STREAK-03
  - STREAK-04

must_haves:
  truths:
    - "EloRepository.calculateElo() calls the calculate_elo RPC and returns EloResult(newElo, rankTier, eloDelta)"
    - "StreakRepository.updateStreak() calls the update_streak RPC and returns StreakResult(streakCount, freezeAvailable, wasFreezeUsed)"
    - "StreakRepository.getStreakHistory(28) returns List<StreakHistoryEntry> from streak_history table"
    - "ProgressRepository.postGame() chains EloRepository + StreakRepository + local state update + syncToCloud"
    - "EloRepository.instance and StreakRepository.instance are set in init{} for use from non-Hilt callsites"
    - "ELO-02: NotificationManager.checkEloMilestone() is called with new ELO after each game"
  artifacts:
    - path: "app/src/main/java/com/example/vietforces/data/repository/EloRepository.kt"
      provides: "ELO RPC client"
      exports: ["EloRepository", "EloResult"]
    - path: "app/src/main/java/com/example/vietforces/data/repository/StreakRepository.kt"
      provides: "Streak RPC + history client"
      exports: ["StreakRepository", "StreakResult", "StreakHistoryEntry"]
    - path: "app/src/main/java/com/example/vietforces/di/GameModule.kt"
      provides: "Hilt module placeholder (auto-bind via @Inject constructor)"
    - path: "app/src/main/java/com/example/vietforces/data/repository/ProgressRepository.kt"
      provides: "postGame() entry point for all post-session server calls"
      contains: "suspend fun postGame"
  key_links:
    - from: "ProgressRepository.postGame()"
      to: "EloRepository.calculateElo()"
      via: "EloRepository.instance?.calculateElo(correct, total, timeMs)"
      pattern: "EloRepository\\.instance"
    - from: "ProgressRepository.postGame()"
      to: "StreakRepository.updateStreak()"
      via: "StreakRepository.instance?.updateStreak()"
      pattern: "StreakRepository\\.instance"
    - from: "EloRepository.calculateElo()"
      to: "supabase.postgrest.rpc(\"calculate_elo\")"
      via: "Supabase Postgrest RPC"
      pattern: "rpc.*calculate_elo"
---

<objective>
Create EloRepository, StreakRepository, and GameModule, then wire `ProgressRepository.postGame()` so that every game session end chains server-side ELO computation, streak update, local state sync, and notification checks in one coroutine call (ELO-01, ELO-02, STREAK-01, STREAK-02 foundation, STREAK-03, STREAK-04 data source).

Purpose: The SQL RPCs from plan 03-01 are now callable from Kotlin. EloRepository and StreakRepository encapsulate the RPC contracts; ProgressRepository.postGame() is the single call site that game screens invoke after session completion.

Output: Three new Kotlin files + one modified file. No new UI. No game screen refactors in this plan — postGame() is wired at one representative callsite (ImageToWordScreen) as proof of integration.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-streak-leaderboard/03-CONTEXT.md
@app/src/main/java/com/example/vietforces/data/repository/ProgressRepository.kt
@app/src/main/java/com/example/vietforces/data/repository/AuthRepository.kt
@app/src/main/java/com/example/vietforces/di/RepositoryModule.kt
@app/src/main/java/com/example/vietforces/di/SupabaseModule.kt
@app/src/main/java/com/example/vietforces/data/manager/NotificationManager.kt
@app/src/main/java/com/example/vietforces/data/model/EloRank.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: EloRepository.kt + StreakRepository.kt + di/GameModule.kt</name>
  <files>
    app/src/main/java/com/example/vietforces/data/repository/EloRepository.kt,
    app/src/main/java/com/example/vietforces/data/repository/StreakRepository.kt,
    app/src/main/java/com/example/vietforces/di/GameModule.kt
  </files>
  <action>
    Create `EloRepository.kt` in `com.example.vietforces.data.repository`:

    - Annotate `@Singleton` class `EloRepository @Inject constructor(private val supabase: SupabaseClient, private val authRepository: AuthRepository)`.
    - Companion object with `var instance: EloRepository? = null`; set `instance = this` in `init {}`.
    - Nested `@Serializable private data class CalculateEloParams` with fields annotated `@SerialName("p_user_id") val userId: String`, `@SerialName("p_correct") val correct: Int`, `@SerialName("p_total") val total: Int`, `@SerialName("p_time_ms") val timeMs: Long`.
    - Public `@Serializable data class EloResult` (top-level or nested) with `@SerialName("new_elo") val newElo: Int`, `@SerialName("rank_tier") val rankTier: String`, `@SerialName("elo_delta") val eloDelta: Int`.
    - `suspend fun calculateElo(correct: Int, total: Int, timeMs: Long): Result<EloResult>`: get `userId` from `authRepository.currentUserId`; if null return `Result.failure(Exception("Not logged in"))`; wrap in try/catch calling `supabase.postgrest.rpc("calculate_elo", CalculateEloParams(userId, correct, total, timeMs)).decodeAs<EloResult>()` inside the try; return `Result.success(result)` on success, `Result.failure(e)` on exception.
    - Imports needed: `io.github.jan.supabase.SupabaseClient`, `io.github.jan.supabase.postgrest.postgrest`, `io.github.jan.supabase.postgrest.rpc`, `kotlinx.serialization.SerialName`, `kotlinx.serialization.Serializable`, `javax.inject.Inject`, `javax.inject.Singleton`.

    Create `StreakRepository.kt` in `com.example.vietforces.data.repository`:

    - Same `@Singleton @Inject constructor(supabase, authRepository)` pattern with `companion object { var instance }` and `init { instance = this }`.
    - Nested `@Serializable private data class UpdateStreakParams` with `@SerialName("p_user_id") val userId: String` and `@SerialName("p_today_date") val todayDate: String`.
    - Public `@Serializable data class StreakResult` with `@SerialName("streak_count") val streakCount: Int`, `@SerialName("streak_freeze_available") val freezeAvailable: Boolean`, `@SerialName("was_freeze_used") val wasFreezeUsed: Boolean`.
    - Public `@Serializable data class StreakHistoryEntry` with `@SerialName("practiced_date") val practicedDate: String` (format "YYYY-MM-DD").
    - Private `fun todayUtcString(): String` using `SimpleDateFormat("yyyy-MM-dd", Locale.ROOT)` with `timeZone = TimeZone.getTimeZone("UTC")` and `format(Date())` — per PRE-01 requirement.
    - `suspend fun updateStreak(): Result<StreakResult>`: get userId; build today string via `todayUtcString()`; call `supabase.postgrest.rpc("update_streak", UpdateStreakParams(userId, today)).decodeAs<StreakResult>()`; return wrapped in Result.
    - `suspend fun getStreakHistory(days: Int = 28): Result<List<StreakHistoryEntry>>`: get userId; compute cutoff date string by subtracting `days` from today using Calendar with UTC timezone and `Locale.ROOT` formatter; call `supabase.from("streak_history").select { filter { eq("user_id", userId); gte("practiced_date", cutoff) }; order("practiced_date", Order.ASCENDING) }.decodeList<StreakHistoryEntry>()`; return Result.success(list) or Result.failure(e).
    - Import `io.github.jan.supabase.postgrest.from`, `io.github.jan.supabase.postgrest.query.Order`.

    Create `di/GameModule.kt` in `com.example.vietforces.di`:

    - Identical skeleton to `RepositoryModule.kt`: `@Module @InstallIn(SingletonComponent::class) object GameModule` with a doc comment: "EloRepository and StreakRepository both use @Singleton @Inject constructor — Hilt auto-binds them. No explicit @Provides needed here."
    - Add `@Binds` for `AuthRepository → AuthRepositoryImpl` if not already bound in `AuthModule.kt`. Check `di/AuthModule.kt` first — if the binding exists there, omit it from GameModule.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>All three files compile. EloRepository and StreakRepository are injectable via Hilt. companion object instance set in init{}.</done>
</task>

<task type="auto">
  <name>Task 2: ProgressRepository.postGame() + wire one game screen</name>
  <files>
    app/src/main/java/com/example/vietforces/data/repository/ProgressRepository.kt,
    app/src/main/java/com/example/vietforces/ui/screens/game/ImageToWordScreen.kt
  </files>
  <action>
    Modify `ProgressRepository.kt`:

    Add a new public data class `PostGameResult(val eloResult: EloRepository.EloResult?, val streakResult: StreakRepository.StreakResult?)` at the top of the file (or as a top-level class in the same package file `PostGameResult.kt` — prefer inline inside ProgressRepository for simplicity).

    Add `suspend fun postGame(correct: Int, total: Int, timeMs: Long): PostGameResult` after the existing `syncIfLoggedIn()` method. Body:

    1. Guard: if `authRepository.currentUserId == null` return `PostGameResult(null, null)`.
    2. Call `val eloResult = EloRepository.instance?.calculateElo(correct, total, timeMs)?.getOrNull()`.
    3. Call `val streakResult = StreakRepository.instance?.updateStreak()?.getOrNull()`.
    4. If `eloResult != null`: call `UserProgressManager.getUserSession().let { session -> session.eloRating = eloResult.newElo; PreferencesManager.saveUserSession(session) }`. Then call `NotificationManager.checkEloMilestone(eloResult.newElo, EloRankUtils.getCurrentRank(eloResult.newElo).name)` (ELO-02: tier change triggers in-app notification).
    5. If `streakResult != null`: set `UserProgressManager.getUserSession().currentStreak = streakResult.streakCount`; call `PreferencesManager.saveUserSession(UserProgressManager.getUserSession())`; call `NotificationManager.addStreakNotification(streakResult.streakCount)`.
    6. Call `syncToCloud()` (fire-and-forget result — ignore errors here since local state is already updated).
    7. Return `PostGameResult(eloResult, streakResult)`.

    Add imports: `com.example.vietforces.data.model.EloRankUtils`, `com.example.vietforces.data.manager.PreferencesManager`.

    Modify `ImageToWordScreen.kt` — wire the postGame call as the proof-of-integration callsite:

    Find the game-completion callback in ImageToWordScreen (look for where `onGameComplete` or `navigateBack` or the results screen is triggered). In the coroutine/LaunchedEffect or ViewModel callback that fires when the game session ends, add: `ProgressRepository.instance?.postGame(correctCount, totalCount, elapsedMs)`. Use the screen's existing `correct`, `total`, and elapsed time tracking variables. If these are not tracked, add local `val startTimeMs = remember { System.currentTimeMillis() }` at the top of the Composable and compute `val elapsedMs = System.currentTimeMillis() - startTimeMs` at completion. Wrap in `rememberCoroutineScope().launch { }` since `postGame` is a suspend function.

    NOTE: Do NOT refactor the existing game screen architecture — only add the single `postGame` call at the natural game-end point. Keep the change minimal.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>ProgressRepository.postGame() exists and compiles. ImageToWordScreen calls postGame at game completion. ELO-01: no ELO delta sent from client — only correct/total/timeMs. ELO-02: NotificationManager.checkEloMilestone called with server-returned newElo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android client → Supabase RPC | Client sends (correct, total, timeMs) for ELO; (today UTC date) for streak |
| Local SharedPreferences write | postGame() writes ELO and streak to local state after receiving server values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | EloRepository params | mitigate | Only correct/total/timeMs sent — server calculates delta; SECURITY DEFINER prevents client from writing elo_score directly |
| T-03-06 | Tampering | StreakRepository todayDate | mitigate | todayUtcString() uses Locale.ROOT + UTC timezone per PRE-01; no client-controlled date manipulation possible beyond today |
| T-03-07 | Repudiation | postGame() call timing | accept | Game completion triggers single postGame(); duplicate same-day streak calls handled by update_streak() no-op |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package manager installs in this plan |
</threat_model>

<verification>
1. `./gradlew :app:compileDebugKotlin` — zero errors.
2. Inspect `EloRepository.kt`: confirm `rpc("calculate_elo", ...)` with `CalculateEloParams`; `companion object { var instance }` set in `init`.
3. Inspect `StreakRepository.kt`: confirm `todayUtcString()` uses `Locale.ROOT`; `getStreakHistory()` queries `streak_history` table with cutoff filter.
4. Inspect `ProgressRepository.kt`: confirm `postGame()` chains EloRepository → StreakRepository → local state update → syncToCloud().
5. Inspect `ImageToWordScreen.kt`: confirm `postGame` is called at game end inside a coroutine scope.
</verification>

<success_criteria>
- EloRepository, StreakRepository compile and are injectable via Hilt
- ProgressRepository.postGame() is the single post-session entry point; no ELO delta is ever sent from the client
- ELO-01: `rpc("calculate_elo")` receives only game metrics; server returns new_elo ✓
- ELO-02: NotificationManager.checkEloMilestone() called with server-returned ELO after each game ✓
- STREAK-01: `rpc("update_streak")` called with UTC today date using Locale.ROOT ✓
- STREAK-03: StreakResult.wasFreezeUsed available for UI display in plan 03-04 ✓
- STREAK-04 data source: StreakRepository.getStreakHistory(28) callable by plan 03-04 heatmap ✓
</success_criteria>

<output>
Create `.planning/phases/03-streak-leaderboard/03-02-SUMMARY.md` when done
</output>
